class User {
  String email, password, address, name, phoneNumber, imageSrc;

  User(
      {this.email,
      this.password,
      this.address,
      this.phoneNumber,
      this.name,
      this.imageSrc});
}
